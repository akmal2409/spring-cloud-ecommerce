spring:
  application:
    name: inventory-service
  cloud:
    config:
      uri: http://localhost:8888
    vault:
      host: localhost
      port: 8200
      scheme: http
      application-name: inventory-service
      authentication: TOKEN

server:
  port: 0




