spring:
  application:
    name: inventory-service
  cloud:
    config:
      uri: http://localhost:8888
    vault:
      host: localhost
      port: 8200
      scheme: http
      authentication: TOKEN
      token: ${VAULT_TOKEN}
      application-name: inventory-service


server:
  port: 0




